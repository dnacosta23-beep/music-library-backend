# Music Library Backend

A REST API built with Flask for the Music Library application.

The backend receives requests from the React frontend, validates the submitted data, communicates with Supabase, and returns JSON responses.

## Technologies Used

- Python
- Flask
- Flask-CORS
- Supabase
- python-dotenv
- Gunicorn
